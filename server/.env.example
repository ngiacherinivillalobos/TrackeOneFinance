# Arquivo de exemplo de variáveis de ambiente para o servidor
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Chave secreta para JWT
JWT_SECRET=sua_chave_secreta_aqui

# URL do banco de dados PostgreSQL (para produção)
DATABASE_URL=postgresql://usuario:senha@host:porta/banco_de_dados

# Porta do servidor (opcional, padrão é 3001)
PORT=3001

# Ambiente (development ou production)
NODE_ENV=development